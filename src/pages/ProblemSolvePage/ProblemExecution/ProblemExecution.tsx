interface ProblemExecutionProps {}
import * as S from './ProblemExecution.style';

export default function ProblemExecution(props: ProblemExecutionProps) {
  return <S.Wrapper>실행 결과가 표시됩니다.</S.Wrapper>;
}
