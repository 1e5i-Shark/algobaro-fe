import styled from 'styled-components';

import { Col } from '@/styles/GlobalStyle';

const Wrapper = styled(Col)``;

const ModalWrapper = styled(Col)`
  gap: 2rem;
  align-items: center;
  padding: 3rem;
`;

const ModalTitle = styled.span``;

export { ModalTitle, ModalWrapper, Wrapper };
